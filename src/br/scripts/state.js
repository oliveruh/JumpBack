'use strict';

const STATE = {
    // --- INDEX Page ---
    birthdate: '',
    errorMessageText: '',

    // --- RESULTS Page ---
    currentMagazineData: null,
    allSeriesData: null,
    activeTab: 'contentsTab',
    genreChartInstance: null,
};

export default STATE;